export interface Itask{
    taskId: string;
    taskName: string;   
    taskDescription: string;
    taskOwner: string;
}

export interface ILogitem{
    logId: string;
    logAuthor: string;
    logMakeDate: string;
    logTimestamp: string;
}